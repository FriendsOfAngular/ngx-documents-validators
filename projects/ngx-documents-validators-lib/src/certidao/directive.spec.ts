import { CertificateValidator } from './directive';

describe('CertificateValidator', () => {
  it('should create an instance', () => {
    const directive = new CertificateValidator();
    expect(directive).toBeTruthy();
  });
});
